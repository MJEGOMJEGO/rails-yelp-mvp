

<h1>All restaurants !!</h1>
<%= link_to "Create a new Restaurant", new_restaurant_path %>
<ul>
<% @restaurants.each do |restaurant| %>
  <li><%= restaurant.name  %>
</li>
<li><%= link_to "See details", restaurant_path(restaurant), class: "btn btn-primary"%></li>
<li><%= link_to "See reviews", restaurant_reviews_path(restaurant), class: "btn btn-warning"%></li>
  <% end %>
</ul>
